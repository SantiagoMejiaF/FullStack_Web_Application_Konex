import { Locality } from "./locality";

export class Client {
    id: number;
    fullName: string;
    idNumber: string;
    address: string;
    phoneNumber: string;
    email: string;
    locality: Locality;
}